use crate::yanrlox::token::Token;
use crate::yanrlox::error::Error;

pub fn scan_tokens(_input: &str) -> Result<Vec<Token>, Error> {
    //Ok(vec!())
    Err(Error::simple_error("This has not been set up yet"))
}
